tries = 0
high_score = 99
while True:
    tries += 1
    try:
        number = input("Please choose a number between 1 and 10")
        number = int(number)
        #tries = 1
        
        #if input != 8:
            #tries += 1

        if number == 8:
            print("You have chosen the secret number!")
            #tries += 1
            print(tries)
            #tries += 1
            break

        elif 0 >= number or number >= 11:
            #tries += 1
            print("Please choose a number BETWEEN 1 AND 10")
    
        else:
            #tries += 1
            print("You did not choose the secret number. Try again!")
         
    except ValueError:
        #tries += 1
        print("Chose a NUMBER, genius!")
        
if tries < high_score and (high_score > 0):
    high_score = tries
    print("New highscore!")